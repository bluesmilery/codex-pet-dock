# AC Evidence Topology - 08-28-pet-window-adaptation

- Product baseline: <pre-rewrite-id> (dev; archived production
  candidate build/candidates/2026-08-28-095217-dev-v0.5.0-<pre-rewrite-id>, live-sampled on the
  updated host ChatGPT.app 26.825.31414 with TCC granted).
- Prior frozen candidate <pre-rewrite-id>: formal Review
  NOT APPROVED (P1=1, P2=2); this artifact and the P2 repairs are part of the next
  candidate commit on codex/pet-window-adaptation (supervisor owns the commit).

| symptom/AC | evidence type | trigger/perturbation | product-baseline full SHA + test state | baseline result | production consumer/path | final owner/assertion or direct guard | command/result | manual-only gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Symptom: after 2026-08-28 host update the dock base no longer shows; pet still visible | behavior + live runtime sampling of the baseline candidate | host auto-update removed the standalone Mascot window; pet is rendered inside a huge near-transparent host overlay container window | <pre-rewrite-id>; baseline candidate (2026-08-28-095217-dev-v0.5.0-<pre-rewrite-id>) live-sampled on 26.825.31414, TCC granted / preflight true; container-channel code and tests absent at baseline | FAIL - symptom present on baseline: tick loop healthy at hidden-state 1s cadence; --diagnose reported selected=none, reason=no-pet:nonmain-notshaped, candidateCount=2 | PetTracker.unionCandidates -> selectPet -> Follower.decide(pet: nil) -> FollowTickPlanner -> dock.hideIfNeeded | DockPanel hidden while pet is visible; baseline --diagnose text is the direct symptom evidence | baseline candidate live sample + --diagnose output (supervisor-verified evidence, cited as-is) | symptom must be shown gone on the frozen-SHA QA run |
| AC1 container-window selection accepts the new-structure candidate and rejects main / helper / gate-failing shapes | unit (pure fixture) | fixture candidate sets: layer-3 huge onscreen non-main container; layer-0 main window; 345x64 helper; offscreen huge; layer-1 huge; multiple matches; equal-area tie; measured fraction band and 0.02 boundary | <pre-rewrite-id>; container tests absent at baseline (red run: undefined ContainerPetSelector) | FAIL at baseline (channel did not exist) | AppDelegate.tick fallback branch consumes ContainerPetSelector.selectContainer(candidates: wins) | selectContainer returns / refuses the expected WinCandidate per fixture (T-cp1..T-cp9, T-cp14..T-cp19); diagnose prints the shared gate value | fresh current-tree make test -> test-ui 523 passed / 0 failed | none (pure function) |
| AC2 alpha bbox -> synthetic pet rect mapping incl. degenerate cases | unit (pure fixture) | golden inputs (121x400 capture, bbox (54,194)-(61,209), container 772x2549 at (1482,-267)); fraction above gate; zero opaque pixels; single-pixel bbox; invalid capture dims | <pre-rewrite-id>; tests absent at baseline (red run) | FAIL at baseline | ContainerPetProbe.locate and its detached completion path consume ContainerPetChannel.captureSize / mapToPetRect | mapped CGRect follows the CGWindowList-bounds-authoritative formula; gates return nil (T-cp13 documents the ~50px SCWindow/CG origin-offset deviation from the design literal numbers) | make test -> test-ui green (T-cp10..T-cp18) | on-device geometry folded into AC6 |
| AC3 hidden -> moving on container observation, moving -> hidden on disappearance, plan outputs unchanged | behavior (probe + planner composition, fake capturer + fake monotonic clock) | first valid observation; disappearance; retention when capture returns targetMissing / unavailable / over-gate stats; 0.33s stable and 0.1s moving cadence; single-flight; reset; wid change | <pre-rewrite-id>; tests absent at baseline (red run) | FAIL at baseline | ContainerPetProbe.locate -> Follower.decide -> FollowTickPlanner.decide (same plan types as the primary channel) | probe outcomes .bounds/.empty/.unavailable and FollowTickPlan flags showUI/resumeData/pauseData/petDisappeared (T-cp20..T-cp69) | make test -> test-ui green | AppDelegate.tick wiring is compile-verified only; live tick loop covered by AC6 |
| AC4 existing PetTracker/Follower/FollowTickPlan tests pass unmodified | regression + static diff | none (existing primary-channel checks unchanged by capture-pivot repair) | <pre-rewrite-id>; pre-existing suites green at baseline acceptance (v0.5.0 candidate archive of <pre-rewrite-id>) | PASS at baseline | unchanged primary chains: selectPet / obstaclesNear / FollowLayoutPass / DockPanel | full suite green with every pre-existing primary-channel check unmodified | fresh current-tree make test -> test-ui 523/0 plus test-data/test-shell/test-docs/test-privacy green | none |
| AC5 swift build -c release 0 warnings; make test green | build | swift build -c release; make test | baseline archive built release per the acceptance flow | PASS | n/a | 0 warnings and all gates green | swift build -c release exit 0 with 0 warnings; make test PYTHON=project .venv exit 0: docs 0 findings, doc tests 10 OK, privacy OK, test-ui green, data/shell green | none |
| AC6 real-device verification on the updated host (launch, host restart, follow at designed cadence, hide on disappearance) | manual QA (deferred for THIS candidate) | launch PetDock; restart host; move pet/container content; toggle bubble/control content; make pet/container disappear and reappear | manual QA runs on the HEAD of the branch containing this file (the commit under review); QA must record git rev-parse HEAD in its evidence and verify it is an ancestor-or-equal descendant chain of <pre-rewrite-id>; prior <pre-rewrite-id> QA PASS for base anchor/disappearance is historical only and is not reused for the R7 contract | THIS candidate R7 QA NOT RUN; historical <pre-rewrite-id> QA PASS for base anchor/disappearance | container channel: CGWindowList union -> ContainerPetSelector -> ContainerPetProbe (ScreenCaptureKit, in-memory alpha only) -> Follower -> DockPanel.placeBelow(empty obstacles); runtime evidence collector records placement and observation edges | dock base reappears below the pet, follows at the designed cadence, hides on disappearance and reappears on recovery; R7 QA must observe live p95 <= 0.4s, one smooth glide <= 200ms, >= 60s steady CPU <= 5%, telemetry cadence, and subjective smoothness; runtime evidence shows containerPlacementShownCount/HiddenCount, containerObservationChangeCount, and --diagnose reports the container channel | THIS candidate: manual QA with --runtime-evidence=<full HEAD SHA> is deferred; historical: <pre-rewrite-id> QA PASS for base anchor/disappearance | ENTIRE ROW manual-only; run on the frozen candidate tree containing this file with --runtime-evidence=<full candidate SHA> |
| QA-failed P0 on candidate <pre-rewrite-id>: container channel never produced a first observation in live mode (hidden-tick reset starvation) | behavior (real-device QA) + regression composition test | container capture in flight while tick is hidden: the disappearance branch called containerProbe.reset() every hidden tick, bumping generation and invalidating each in-flight capture; device evidence: PetDock owned 0 onscreen windows after 9+ minutes, evidence JSON never gained container counters (pet bbox itself was recovered: 93 opaque px, fraction 0.0019) | <pre-rewrite-id> (QA candidate); regression tests absent at that SHA (red run: undefined FollowTickPlanner.containerProbeReset before fix) | FAIL on real device | tick disappearance branch -> containerProbe.reset() -> generation++ -> in-flight capture discarded -> locate stays .empty forever | FollowTickPlanner.containerProbeReset(containerCandidatePresent:) gates the reset to absent-candidate ticks only; T-cp70-T-cp75 pin helper semantics, the starvation-fix composition (in-flight capture lands as .bounds after a hidden tick with candidate present), reset still invalidating cache when the candidate is absent, and wake-once semantics | real-device QA FAIL at <pre-rewrite-id> (supervisor-reported); historical repair batch was 498/0 (container 78/0); fresh current-tree capture-pivot gate is swift build release 0 warnings, make test green, test-ui 523/0 (container section 100/0) | re-run real-device QA on the next frozen SHA (first observation must land in live mode; evidence JSON gains container counters) |
| QA-run-4: AC8 CPU acceptance failed at <pre-rewrite-id>, and the 0.01 opaque gate flickered | real-device QA + behavior (fake stream lifecycle/latest-wins) + unit gate | 60s channel-active CPU sampling with display awake returned 12.02% average (6.6–15.9% samples); opaque fraction crossed the old gate across frames (first diagnose rejected at 0.010227, later passed at 0.009008); pivot trigger is container selection, reset-with-absence, WID change, permission loss, and shutdown | <pre-rewrite-id>; QA evidence valid and display awake; red-first gate run at this SHA gave 508/3 (T-cp16/T-cp16b/T-cp19 FAIL), and stream tests then failed compile against the absent transport API | FAIL real-device CPU at <pre-rewrite-id>; gate red-first FAIL at the same base | AppDelegate managed transport -> ContainerPetProbe unique stream lifecycle -> SCStreamConfiguration(max side 400, minimumFrameInterval 0.33) -> background bbox consumer latest-wins gate -> accepted-observation edge -> scheduler/DockPanel; macOS13 remains one-shot at 1.0s | Fake stream factory owns start/stop counts and injected outcomes: T-cp83 one live stream per WID, T-cp84/T-cp85 unchanged edges, T-cp86/T-cp87 drop while computing, T-cp88 stop on absence, T-cp89 stop/restart on WID change, T-cp91 stop on permission loss, T-cp92 stop on shutdown; T-cp16..T-cp19 pin gate 0.02 and diagnose prints the shared constant | QA-run-4 FAIL: CPU 12.02% average at <pre-rewrite-id>; repair-tree red run 508/3; repair-tree green gate swift build release 0 warnings and test-ui 523/0 (container 100/0) | next QA must re-measure live CPU on the frozen HEAD-of-branch tree, record git rev-parse HEAD and an ancestor-or-equal descendant chain of <pre-rewrite-id>, and verify p95/telemetry/smoothness |
| R7 / AC7 / AC8: user-reported smoothness regression on bubble/control toggle; container content change must re-anchor with <=0.4s p95 detection, one <=200ms glide, and <=5% steady CPU | behavior (fake-clock edge + cadence + coalescer) and sanitized telemetry unit; device QA deferred | accepted stream frames / capture changes: none->bounds, bounds->same, bounds->different; reset re-arms baseline; stable stream interval is 0.33s; RuntimeEvidence records observation edges | <pre-rewrite-id>; worktree clean before repair cycle 3; red-first run at this HEAD failed compile because onObservationChanged / recordContainerObservationChange were absent, and the old stable interval was 1.0; <pre-rewrite-id> was the prior responsive-channel repair tree | FAIL red-first at <pre-rewrite-id>; PASS after repair; CPU contract moved to QA-run-4 after <pre-rewrite-id> measured 12.02% | ContainerPetProbe accepted stream frame -> rect edge -> onObservationChanged -> AppDelegate runtimeEvidence record + FollowTickScheduler.requestWake -> FollowTickCoalescer -> tick -> DockPanel.placeBelow | Final owner/assertion: each accepted rect edge produces exactly one callback and one coalesced wake/drain in T-cp76..T-cp79; RuntimeEvidence edge count/flush is owned by T-re2j..T-re2l; the production placement owner is DockPanel.placeBelow | Cadence guard: T-cp80 pins 0.33s, T-cp81..T-cp82 assert the one-shot boundary, T-cp93 pins stream minimumFrameInterval 0.33s, and T-cp94 pins macOS13 fallback at 1.0s; fresh gates: swift build release 0 warnings and test-ui 523/0 | AC8 live p95 latency, >=60s CPU, telemetry cadence, disappearance/reappearance, and subjective smoothness require manual QA on the frozen candidate tree containing this file; AppDelegate wiring is release-compile verified |
