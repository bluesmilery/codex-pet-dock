# AC Evidence Topology - 08-28-pet-window-adaptation

- Product baseline: <pre-rewrite-id> (dev; archived production
  candidate build/candidates/2026-08-28-095217-dev-v0.5.0-<pre-rewrite-id>, live-sampled on the
  updated host ChatGPT.app 26.825.31414 with TCC granted).
- Prior frozen candidate <pre-rewrite-id>: formal Review
  NOT APPROVED (P1=1, P2=2); this artifact and the P2 repairs are part of the next
  candidate commit on codex/pet-window-adaptation (supervisor owns the commit).

| symptom/AC | evidence type | trigger/perturbation | product-baseline full SHA + test state | baseline result | production consumer/path | final owner/assertion or direct guard | command/result | manual-only gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Symptom: after 2026-08-28 host update the dock base no longer shows; pet still visible | behavior + live runtime sampling of the baseline candidate | host auto-update removed the standalone Mascot window; pet is rendered inside a huge near-transparent host overlay container window | <pre-rewrite-id>; baseline candidate (2026-08-28-095217-dev-v0.5.0-<pre-rewrite-id>) live-sampled on 26.825.31414, TCC granted / preflight true; container-channel code and tests absent at baseline | FAIL - symptom present on baseline: tick loop healthy at hidden-state 1s cadence; --diagnose reported selected=none, reason=no-pet:nonmain-notshaped, candidateCount=2 | PetTracker.unionCandidates -> selectPet -> Follower.decide(pet: nil) -> FollowTickPlanner -> dock.hideIfNeeded | DockPanel hidden while pet is visible; baseline --diagnose text is the direct symptom evidence | baseline candidate live sample + --diagnose output (supervisor-verified evidence, cited as-is) | symptom must be shown gone on the frozen-SHA QA run |
| AC1 container-window selection accepts the new-structure candidate and rejects main / helper / gate-failing shapes | unit (pure fixture) | fixture candidate sets: layer-3 huge onscreen non-main container; layer-0 main window; 345x64 helper; offscreen huge; layer-1 huge; multiple matches; equal-area tie | <pre-rewrite-id>; container tests absent at baseline (red run: undefined ContainerPetSelector) | FAIL at baseline (channel did not exist) | AppDelegate.tick fallback branch consumes ContainerPetSelector.selectContainer(candidates: wins) | selectContainer returns / refuses the expected WinCandidate per fixture (T-cp1..T-cp9) | make test -> test-ui 498 passed / 0 failed | none (pure function) |
| AC2 alpha bbox -> synthetic pet rect mapping incl. degenerate cases | unit (pure fixture) | golden inputs (121x400 capture, bbox (54,194)-(61,209), container 772x2549 at (1482,-267)); fraction above gate; zero opaque pixels; single-pixel bbox; invalid capture dims | <pre-rewrite-id>; tests absent at baseline (red run) | FAIL at baseline | ContainerPetProbe.locate and its detached completion path consume ContainerPetChannel.captureSize / mapToPetRect | mapped CGRect follows the CGWindowList-bounds-authoritative formula; gates return nil (T-cp13 documents the ~50px SCWindow/CG origin-offset deviation from the design literal numbers) | make test -> test-ui green (T-cp10..T-cp18) | on-device geometry folded into AC6 |
| AC3 hidden -> moving on container observation, moving -> hidden on disappearance, plan outputs unchanged | behavior (probe + planner composition, fake capturer + fake monotonic clock) | first valid observation; disappearance; retention when capture returns targetMissing / unavailable / over-gate stats; 1s stable and 0.1s moving cadence; single-flight; reset; wid change | <pre-rewrite-id>; tests absent at baseline (red run) | FAIL at baseline | ContainerPetProbe.locate -> Follower.decide -> FollowTickPlanner.decide (same plan types as the primary channel) | probe outcomes .bounds/.empty/.unavailable and FollowTickPlan flags showUI/resumeData/pauseData/petDisappeared (T-cp20..T-cp69) | make test -> test-ui green | AppDelegate.tick wiring is compile-verified only; live tick loop covered by AC6 |
| AC4 existing PetTracker/Follower/FollowTickPlan tests pass unmodified | regression + static diff | none (production files untouched; tests/main.swift append-only) | <pre-rewrite-id>; pre-existing suites green at baseline acceptance (v0.5.0 candidate archive of <pre-rewrite-id>) | PASS at baseline | unchanged primary chains: selectPet / obstaclesNear / FollowLayoutPass / DockPanel | full suite green with every pre-existing check unmodified (diff reviewed as append-only) | make test -> test-ui 498/0 plus test-data/test-shell/test-docs/test-privacy green | none |
| AC5 swift build -c release 0 warnings; make test green | build | swift build -c release; make test | baseline archive built release per the acceptance flow | PASS | n/a | 0 warnings and all gates green | swift build -c release exit 0 with 0 warnings; make test PYTHON=project .venv exit 0: docs 0 findings, doc tests 10 OK, privacy OK, test-ui green, data/shell green | none |
| AC6 real-device verification on the updated host (launch, host restart, follow at designed cadence, hide on disappearance) | manual QA (deferred) | launch PetDock; restart host; move pet/container content; make pet/container disappear | frozen SHA of the next candidate (post-repair; prior candidate <pre-rewrite-id> NOT APPROVED P1=1/P2=2, repairs uncommitted in worktree) | NOT RUN | container channel: CGWindowList union -> ContainerPetSelector -> ContainerPetProbe (ScreenCaptureKit, in-memory alpha only) -> Follower -> DockPanel.placeBelow(empty obstacles) | dock base reappears below the pet, follows at the designed cadence, hides on disappearance; runtime evidence shows containerPlacementShownCount/HiddenCount and --diagnose reports the container channel | deferred | ENTIRE ROW manual-only -> QA on the frozen SHA with --runtime-evidence=<full sha> |
| QA-failed P0 on candidate <pre-rewrite-id>: container channel never produced a first observation in live mode (hidden-tick reset starvation) | behavior (real-device QA) + regression composition test | container capture in flight while tick is hidden: the disappearance branch called containerProbe.reset() every hidden tick, bumping generation and invalidating each in-flight capture; device evidence: PetDock owned 0 onscreen windows after 9+ minutes, evidence JSON never gained container counters (pet bbox itself was recovered: 93 opaque px, fraction 0.0019) | <pre-rewrite-id> (QA candidate); regression tests absent at that SHA (red run: undefined FollowTickPlanner.containerProbeReset before fix) | FAIL on real device | tick disappearance branch -> containerProbe.reset() -> generation++ -> in-flight capture discarded -> locate stays .empty forever | FollowTickPlanner.containerProbeReset(containerCandidatePresent:) gates the reset to absent-candidate ticks only; T-cp70-T-cp75 pin helper semantics, the starvation-fix composition (in-flight capture lands as .bounds after a hidden tick with candidate present), reset still invalidating cache when the candidate is absent, and wake-once semantics | real-device QA FAIL at <pre-rewrite-id> (supervisor-reported); repair batch: swift build -c release 0 warnings, make test green, test-ui 498/0 (container section 78/0) | re-run real-device QA on the next frozen SHA (first observation must land in live mode; evidence JSON gains container counters) |
